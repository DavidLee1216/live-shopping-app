name: liveapp
description: Live shopping streaming app

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 0.1.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  camera: ^0.5.8+11
  path_provider: ^1.6.24
  path: ^1.7.0
  video_player: ^1.0.1

  # Third party
  flutter_svg: ^0.19.1
  fluttertoast: ^7.1.3
  random_string: ^2.1.0
  permission_handler: ^5.0.1+1
  liquid_pull_to_refresh: ^2.0.0

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Official
  http: ^0.12.2
  crypto: ^2.1.5
  shared_preferences: ^0.5.12+4

  flutter_launcher_icons: ^0.8.1

flutter_icons:
  android: launcher_icon
  ios: true
  image_path: assets/images/appicon.png

flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/

  fonts:
    - family: NanumSquare
      fonts:
        - asset: assets/fonts/NanumSquareRegular.ttf
        - asset: assets/fonts/NanumSquareLight.ttf
        - asset: assets/fonts/NanumSquareBold.ttf
        - asset: assets/fonts/NanumSquareExtraBold.ttf
    - family: GmarketSans
      fonts:
        - asset: assets/fonts/GmarketSansTTFBold.ttf
        - asset: assets/fonts/GmarketSansTTFLight.ttf
        - asset: assets/fonts/GmarketSansTTFMedium.ttf
    - family: NEXONLv2Gothic
      fonts:
        - asset: assets/fonts/NEXONLv2GothicBold.ttf
        - asset: assets/fonts/NEXONLv2GothicLight.ttf
        - asset: assets/fonts/NEXONLv2GothicMedium.ttf
        - asset: assets/fonts/NEXONLv2Gothic.ttf
